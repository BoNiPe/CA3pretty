extends layout

block content
  div.container

    h1= title
    p #{title}
    table(border=1)
        tbody
            each val in orders
                tr
                    td #{val.orderId}
                    td #{val.productId}
                    td #{val.unitPrice}
                    td #{val.quantity}
                    td #{val.discount}
